---
title: "SML Playground"
---

# SML Playground { ❄ }

Parse SML right in your browser — with contract validation, fragment inheritance
and include escaping.

Defaults to the JavaScript implementation (`sml.mjs`, zero-dependency ESM, instant).
Switch to **Rust (wasm, faithful)** with one click — identical to production, it
preserves `1.0`, leading zeros and 18-digit numbers that JSON views tend to lose.

{{< sml-playground >}}
