# Rust
target/
# 本地校验/验证用构建目录（cargo 验证产物，非源码）
rust/target_verify/
Cargo.lock
# 例外：**主 Rust workspace 的锁文件必须入库**（Cargo 官方建议：含二进制 crate 的
# workspace 提交 Cargo.lock）。原因不是洁癖，是两条实打实的：
#   1) `rust/osv_check.py` 在导入时就读 `<repo>/rust/Cargo.lock`；CI 的 `osv` job 是
#      **独立 checkout**，没有锁文件 ⇒ 直接 FileNotFoundError ⇒ 该 job 必红（红的
#      原因还不是漏洞，纯属误导）；
#   2) 不锁版本 ⇒ CI 每次都解析"当天最新"的依赖，与本地测过的不是同一套，
#      OSV 门禁也就查的不是你实际用的那一份。
# 其余 crate 的锁文件（如 editors/zed/grammars/sml/Cargo.lock）继续忽略。
!rust/Cargo.lock

# 构建产物
*.sar
out_verify/
out/

# Python
__pycache__/

# 构建/测试临时产物（C / C++ 编译输出与日志）
*.exe
*.txt
# 但保留仓库根的 llms.txt（面向 AI 的引导文件，需进版本库与官网）
!llms.txt
!site/static/llms.txt
*.dll
!logo/*.dll
inc_tmp/

# Dioxus 站点（独立未发布）
site-dioxus/

# Wrangler 运行时缓存与本机日志
site/.wrangler/
site/dev.log
site/build_site.log
site/hugo_server.log
site/hugo_server.err

# 本地凭据文件（含密钥，切勿提交）—— 通用规则，不绑定具体服务商。
# ⚠️ 必须包含 `*.env`：`<服务商>.env` 这类「前缀 + .env 后缀」的命名只有它能匹配。
# （曾因漏掉这条，把本机真实凭据文件提交进了仓库。）
.env
.env.*
*.env
*.env.local

# 临时/调试脚本（非库代码，勿提交）
_du.py

# AI 协作工具的本地状态（团队/会话数据），不进版本库
.codebuddy/
c/build_check.py

# Hugo 生成的站点产物（由 site/content 与 site/static 重新生成，勿提交）
site/public/
# 但保留前端运行必需的静态资源（同源于 site/static 或由构建脚本产出一次）
!site/public/llms.txt
!site/public/sml.mjs
!site/public/sml.svg
!site/public/sml-verify.js
!site/public/.gitkeep

# 报告与打包产物（安全扫描报告、电子书导出等）
swsml-*.zip
sml-book.epub

# 私有报送件 / 内部报告（不进版本库）
#
# ⚠️ 2026-09-18 起，这些文件的**正主**是内网可信服务器上的私有库：
#      http://10.16.144.2:3000/CrystalicCore/sml_secret.git
#    工作副本放在**仓库之外**（Windows: C:\Users\<你>\Desktop\sml_secret），
#    主库工作区里**不放**这些文件了（放了就有手滑提交进公开库的机会）。
#    下面几条**故意保留**：不是给文件找地方放，而是「万一有人放进工作区」时挡一道。
#    守卫：`python tools/check_private_assets.py`（查历史 / 索引 / 这几条规则在位）。
SML_政务数据密级标注规范_报送稿.*
SML图形管线项目_全量文档合集.md
SML_数字字面量保真性审计报告.md
报送邮件.txt

# PVACIS 对 SML 的方言扩展说明：PVACIS 仍在开发，待投入使用并做好开源准备前不对外
# （待解禁：删除本行即可入库）
PVACIS-SML-EXTENSIONS.md

# 未公开组件：已剥离到私密仓库（sml-private），公开仓库不再跟踪，历史已重写清除。
# 本地若仍留有副本，这两行确保 `git add -A` 也不会把它们加回来。
rust/qsm/
rust/crystalic/

# 文档演示产物与 AI 生成图（由 examples/doc-demo 与 image_gen 产出，勿提交）
examples/doc-demo/
generated-images/

# 漏洞扫描报告等（敏感，勿提交）
vex.zip
swsml-*.zip

# 构建/生成产物
build/settings/
rust/qsm/target-*/
rust/qsm/web/pkg/
node_modules/

# 任意层级的临时/调试脚本（_ 开头，非库代码，勿提交）
**/_*

# ── `**/_*` 的精确例外 ────────────────────────────────────────────────────
# 上面这条规则的本意是挡「本机临时探针」，但它连带挡掉了下面这些**必须入库**的文件。
# 判据不是「文件名像不像临时脚本」，而是「有没有被引用 / 是不是纯产物」，
# 每条单独注明理由。**凡自述「一次性 / 临时（_ 开头不入库）」或硬编码本机绝对路径的
# 一律不放行**（tools/_*、site/tools/_*、story/_validate_novel.py、
# editors/vscode/scripts/_probe*.py 均属此类，维持忽略）。

# 1) Cloudflare Pages 的部署配置：static/_headers 里是跨域放行规则（CORS / CORP /
#    Content-Type）。Hugo 构建时会把 static/ 原样拷到 public/，Pages 再据此发响应头。
#    它**本身就是站点构建的输入**，丢了它 ⇒ 换机器或 CI 构出来的站点没有这批响应头，
#    外部站点 import "https://sml.swebase.cn/lib/sml.mjs" 会被 CORS 拦掉。
#    引用处：site/build_site.py:26（「放行头见 static/_headers」）、TODO.md:242。
!site/static/_headers

# 2) tree-sitter python 绑定的包入口（tree-sitter generate 产出，共 5 行）。
#    同目录的 binding.c 与 py.typed 已经入库，唯独缺 __init__.py ⇒
#    `import tree_sitter_sml` 直接失败，仓库里的 python 绑定是残的；
#    同理缺 .pyi ⇒ 类型提示丢失。其它语言绑定（go/node/rust/swift）都已入库。
!editors/zed/grammars/sml/bindings/python/tree_sitter_sml/__init__.py
!editors/zed/grammars/sml/bindings/python/tree_sitter_sml/__init__.pyi

# 3) VSCode 扩展的发布闸门与校验脚本（手写的持久工具，非一次性探针）。
#    它们不引入新的安全风险，也不含本机绝对路径；缺了它们「发布前在真实 Oniguruma
#    引擎上验证高亮」这道硬门槛就无法复现。注意：目前仓库里**尚未有任何文件引用**
#    它们（package.json 只引用 scripts/sync-parser.py），是否保留由后续决定。
!editors/vscode/scripts/_prepublish.mjs
!editors/vscode/scripts/_verify_grammar.mjs
!editors/vscode/scripts/_verify_tokenize.mjs
!editors/vscode/scripts/_verify_hlcfg.mjs
!editors/vscode/scripts/_verify_ext.mjs
!editors/vscode/scripts/_verify_activate.mjs
!editors/vscode/scripts/_gen_seti_theme.py

# ── 用户 / 工作区本地产物（不属于仓库）──────────────────────────────────────

# SML 自定义高亮配置：由扩展命令「SML: 应用特殊颜色 / 创建 HL-cfg.sml」在**工作区根**生成，
# 内容是**用户自己的**词 → 颜色映射（个人偏好，不是项目资产）。2026-09-19 曾被
# `git add -A` 误提交一次（随即 `git rm --cached`），故在此挡掉。
HL-cfg.sml

